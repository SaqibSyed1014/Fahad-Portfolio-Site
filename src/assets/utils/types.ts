import exp from "constants";

export interface Skill {
    title: string,
    description: string,
    icon: string,
    rating: number
}

export interface Tool {
    title: string,
    icon: string
}
